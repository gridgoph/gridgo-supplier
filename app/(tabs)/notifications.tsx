import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

/** SLA deadlines and state changes, push and in-app. */
export default function NotificationsScreen() {
  return <ScreenPlaceholder name="Notifications" />;
}
