import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

/** Profile, addresses, payment method, theme preference, and sign out. */
export default function AccountScreen() {
  return <ScreenPlaceholder name="Account" />;
}
