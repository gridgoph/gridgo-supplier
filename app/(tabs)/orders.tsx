import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

/** Order history, active delivery tracking, and the 24-hour issue window. */
export default function OrdersScreen() {
  return <ScreenPlaceholder name="Orders" />;
}
