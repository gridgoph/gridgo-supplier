import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

/** Entry to the 4-step print request: Details → Artwork → Review → Confirm. */
export default function NewRequestScreen() {
  return <ScreenPlaceholder name="New Request" />;
}
